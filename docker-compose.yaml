version: "3"
services:
  domoticz:
    container_name: domoticz
    build: ./
    volumes:
       - backup:/home/domoticz/stable/backup
    environment:
       - "TZ=Europe/Minsk"       
    restart: unless-stopped
    network_mode: "host"
    privileged: true
volumes:
  backup: